// Intentionally empty. The previous entries were unattributable quotes paired with
// mismatched stock avatars, so they were removed rather than rebranded.
//
// To re-enable the section: add real, permission-cleared client quotes here, drop
// the matching avatar in `public/clients/`, restore `Testimonial: 'testimonial'` to
// `data/common/menus.js`, and render <Testimonial /> in `pages/index.jsx`.
//
// Shape: { name: string, avatar: string, description: string }
const testimonials = []

export default testimonials
